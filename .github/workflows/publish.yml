name: Publiser JAR

on:
  push:
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENCE"
      - "CODEOWNERS"
    branches:
      - main
      - jackson3

jobs:
  publish-jar:
    permissions:
      contents: read
      packages: write
    uses: navikt/hm-workflows/.github/workflows/publish-jar.yaml@main
