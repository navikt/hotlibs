name: Publiser JAR

on:
  push:
    branches:
      - main

jobs:
  publish-jar:
    permissions:
      contents: read
      packages: write
    uses: navikt/hm-workflows/.github/workflows/publish-jar.yaml@main
